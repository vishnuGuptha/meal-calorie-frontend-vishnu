First read and execute the /backend/README.md 
then  
below commands in the {/frontend} location

```bash
npm install
npm run dev
```

afterwards - Open [http://localhost:3000](http://localhost:3000) with your browser to see the result.

